{
  "scripts": {
    "prod": "rm -fR build && wails build && open ./build/bin/*.app",
    "dev": "wails dev 2>&1 | grep -v 'AssetHandler'",
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
